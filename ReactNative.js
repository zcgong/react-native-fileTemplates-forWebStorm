/**
 * ${NAME}
 * Created by ${USER} on ${DATE}.
 */
 "use strict";
 import React,{
    Component,
    View,
    Text,
 } from 'react-native';


 class ${NAME} extends Component {
    // 默认属性
    static defaultProps = {};

    // 属性类型
    static propTypes = {};

    // 构造
    constructor(props) {
        super(props);
        // 初始状态
        this.state = {};
    }

    // 渲染
    render() {
        return (
            <View>
                 <Text>${NAME}</Text>
            </View>
        );
    }

}

export default ${NAME};
